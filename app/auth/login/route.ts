import { NextRequest, NextResponse } from 'next/server';
import { createOidcTransaction, identityIssuer, transactionCookie } from '@/lib/identity/session';
export async function GET(request: NextRequest) { const { transaction, encoded, challenge } = createOidcTransaction(request, request.nextUrl.searchParams.get('returnTo') || '/'); const authorize = new URL(`${identityIssuer()}/auth`); authorize.search = new URLSearchParams({ client_id: 'chat', redirect_uri: transaction.redirectUri, response_type: 'code', scope: 'openid profile email member:summary chat:read chat:write', state: transaction.state, nonce: transaction.nonce, code_challenge: challenge, code_challenge_method: 'S256' }).toString(); const response = NextResponse.redirect(authorize, 303); response.headers.set('Cache-Control', 'no-store'); response.headers.append('Set-Cookie', transactionCookie(encoded, request, 600)); return response; }
