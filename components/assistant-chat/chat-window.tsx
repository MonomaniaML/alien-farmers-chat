import type { AssistantConversation, ChatAssistant, QuickAction } from '@/lib/assistant-chat/types';
import { AssistantHeader } from './assistant-header';
import { MessageList } from './message-list';
import { MessageComposer } from './message-composer';
export function ChatWindow({assistant,conversation,typing,newMessageId,supportOnline,onBack,onDraft,onSend,onAction,onToggleSupport,cloudPersisted=false}:{assistant:ChatAssistant;conversation:AssistantConversation;typing:boolean;newMessageId:string|null;supportOnline:boolean;onBack:()=>void;onDraft:(value:string)=>void;onSend:()=>void;onAction:(item:QuickAction)=>void;onToggleSupport:()=>void;cloudPersisted?:boolean}){return <section className="assistant-chat-window"><AssistantHeader assistant={assistant} conversation={conversation} supportOnline={supportOnline} onBack={onBack} onToggleSupport={onToggleSupport}/><MessageList messages={conversation.messages} typing={typing} onAction={onAction} newMessageId={newMessageId}/><MessageComposer assistantId={assistant.id} value={conversation.draft} onChange={onDraft} onSend={onSend} privateChannel={assistant.private} cloudPersisted={cloudPersisted}/></section>;}
